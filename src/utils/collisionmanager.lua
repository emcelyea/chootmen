local collisionmanager = {}

function detect(b1, b2)
	if b1.x > b2.x and b1.x < b2.x + b2.width
end
function collisionmanager.laserToEnemy(enemies, lasers)
	for key, enemy in pairs(enemies) do
		for k, laser in pairs(lasers) do

		end
	end
end

function collisionmanager.laserToPlayer(player, lasers)
end
function collisionmanager.enemyToPlayer(player, enemies)
end

return collisionmanager